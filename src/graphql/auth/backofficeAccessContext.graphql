query backofficeAccessContext {
	backofficeAccessContext {
		platform_permissions
		shelters {
			shelter {
				id
				name
				city
				type
			}
			membership_status
			roles
			permissions
			access_mode
			platform_override_active
		}
	}
}
