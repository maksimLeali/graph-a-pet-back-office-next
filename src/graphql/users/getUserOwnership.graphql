query getUserOwnership($id: ID!, $commonSearch: CommonSearch!){
    getUser(id: $id){
        user {
            ownerships(commonSearch: $commonSearch)
            {
                ...UserOwnerships
            }
        }
        success
        error {
            code
            message
            extra
        }
    }
}