query getPaginatedPets($search: CommonSearch!) {
	listPets(commonSearch: $search) {
		pagination {
			total_items
			total_pages
			current_page
			page_size
		}
		success
		error {
			message
			code
			extra
		}
		items {
			...SimplePet
		}
	}
}
